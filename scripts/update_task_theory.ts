import { db } from '@/db';
import { tasks } from '@/db/schema';
import { eq } from 'drizzle-orm';

async function main() {
    const fullDescription = `# 💎 1.1 Структура тесту: Від першого рядка до запуску

Уявіть, що тестування UI — це керування роботом, який на 100% точно повторює дії користувача: клікає, друкує, переглядає сторінки та перевіряє результати.

## 🚦 Синхронність vs Асинхронність
Перш ніж писати код, важливо зрозуміти одну річ: **робота в браузері — це асинхронний процес.**

Уявіть, що ви замовляєте каву:
*   **Синхронно**: Ви стоїте біля каси, поки бариста готує напій. Ви нічого не можете робити, поки не отримаєте каву (програма "зависає").
*   **Асинхронно**: Ви замовили каву, отримали пейджер і сіли за стіл працювати. Коли кава готова — пейджер завібрував (програма продовжує працювати, не чекаючи).

У Playwright майже все — це **асинхронні** дії. Відкриття сайту, клік по кнопці, пошук елемента — ці кроки займають час. Тому ми використовуємо спеціальні ключові слова:
1.  **\`async\`** — каже програмі: "Ця функція містить асинхронні кроки, не чекай на її миттєве завершення".
2.  **\`await\`** — каже програмі: "Зупинись тут і почекай, поки цей конкретний крок (наприклад, завантаження сторінки) дійсно завершиться".

Без \`await\` ваш тест намагатиметься клікнути по кнопці ще до того, як сторінка взагалі відкриється.

## 🏗️ Як працює тест?
Будь-який автоматизований тест — це міні-сценарій. У ньому завжди є три кроки (патерн **Arrange - Act - Assert**):

1.  **Підготовка (Arrange):** Відкрити сторінку, підготувати тестові дані.
2.  **Дія (Act):** Натиснути кнопку, ввести текст.
3.  **Перевірка (Assert):** Переконатися, що результат правильний (з’явився текст успішної реєстрації).

**У Playwright цей сценарій оформляється всередині функції \`test()\`:**
\`\`\`javascript
test('опис тесту', async ({ page }) => {
  // 1. Arrange: йдемо на сайт
  await page.goto('https://example.com');
  
  // 2. Act: тиснемо кнопку
  await page.click('button#login');
  
  // 3. Assert: перевіряємо текст
  await expect(page.locator('.status')).toHaveText('Success');
});
\`\`\`

---
### ? Ваше завдання
Давайте почнемо з найпростішого. Оголосіть порожній тест з назвою **'login test'**, що отримує об'єкт \`{ page }\`. Всередині тесту виведіть у консоль повідомлення \`console.log('Початок тесту!');\`.`;

    await db.update(tasks)
        .set({ description: fullDescription })
        .where(eq(tasks.id, 91));

    console.log('Successfully updated Task 91 with detailed Sync/Async theory.');
}

main().catch(console.error);
