export const level1Tasks = [
    {
        title: "1.0 Вступ: Як впровадити автотестування в компанії",
        description: `## 🚀 Автоматизація з нуля: Детальний гайд для QA

Впровадження автоматизації — це не просто написання коду. Це зміна процесів у всій компанії. Часто ініціативи з автоматизації провалюються, бо інженери фокусуються на фреймворках, а не на потребах бізнесу.

Ось детальний покроковий план, як зробити це правильно і отримати підтримку керівництва:

### 📊 Крок 1: Аналіз та обґрунтування (Business Value)
Бізнес розмовляє мовою грошей та часу. Щоб отримати ресурси на автотести, ви маєте "продати" цю ідею.
- **Аудит ручного тестування:** Порахуйте, скільки годин команда витрачає на регресійне тестування перед кожним релізом. Якщо це 20 годин на тиждень — це 80 годин на місяць. Переведіть це в гроші (зарплата QA).
- **Time-to-Market:** Поясніть, що ручне тестування затримує вихід нових фіч. Автотести дозволять релізити не раз на 2 тижні, а щодня.
- **Визначення "Критичного шляху" (Critical Path):** Не намагайтеся покрити 100% сайту. Знайдіть 20% функціоналу, який приносить 80% грошей компанії (наприклад: Авторизація, Додавання в кошик, Оплата). Саме ці флоу потребують тестів в першу чергу.

### 🛠 Крок 2: Proof of Concept (PoC - Доказ концепції)
Не просіть у менеджменту "місяць на налаштування фреймворку". Зробіть це швидко:
1. Візьміть сучасний інструмент (наприклад, **Playwright**).
2. Напишіть 3-5 найголовніших тестів (Smoke tests) за кілька днів.
3. Продемонструйте їх роботу всій команді. Покажіть, як швидко (за секунди) проходять тести, які займали у вас 10 хвилин ручної перевірки.

### 🔄 Крок 3: Інтеграція в CI/CD (Continuous Integration)
**Найбільша помилка:** Тести запускаються тільки локально на ноутбуці автоматизатора.
Автотести мають бути "охоронцями" (Gatekeepers) вашого коду:
- Налаштуйте GitHub Actions, GitLab CI або Jenkins.
- Тести повинні запускатися автоматично при кожному створенні Pull Request (коли розробник хоче додати новий код).
- Якщо 1 тест падає — система блокує мерж (злиття) коду. Таким чином баги просто не можуть потрапити на продакшн.

### 🧱 Крок 4: Формування культури (Quality Engineering)
Якість — це справа всієї команди, а не тільки одного відділу.
- **Допомога від розробників:** Вони мають додавати спеціальні локатори в код (наприклад, \`data-testid="submit-button"\`). Це зробить ваші тести в 10 разів стабільнішими.
- **Аналіз падінь:** Розробники теж повинні вміти читати звіти тестів (Allure/Playwright Report) і розуміти, чому їхній код зламав автотест.
- **Підтримка:** Автотести вимагають постійної підтримки (Maintenance). Виділяйте на це 20% часу в кожному спринті.

### 📉 Топ-3 помилки при старті:
1. **Писати UI тести для всього** (Спочатку покривайте API, а UI тестуйте тільки для головних користувацьких сценаріїв — "Піраміда тестування").
2. **Робити тести залежними один від одного** (Кожен тест має бути незалежним і сам готувати для себе дані).
3. **Ігнорувати flaky tests** (Тести, які іноді проходять, а іноді падають без причини. Вони вбивають довіру команди до автоматизації, їх треба фіксити негайно або видаляти).

---
### 🎯 Ваше завдання
Який функціонал застосунку слід покривати автотестами **в першу чергу** під час ініціального впровадження автоматизації в компанії?
`,
        code: "",
        type: "quiz",
        options: [
            "Усі існуючі функції додатку (100% test coverage) для ідеальної надійності",
            "Складні edge-кейси та рідкісні баги, щоб їх більше не пропускати",
            "Критичний шлях (Critical Path / Smoke tests) - 20% функціоналу, що генерує 80% цінності для бізнесу (напр. Login, Checkout)",
            "Тільки новий функціонал, що розробляється в поточному спринті"
        ],
        correctAnswer: "Критичний шлях (Critical Path / Smoke tests) - 20% функціоналу, що генерує 80% цінності для бізнесу (напр. Login, Checkout)"
    },
    {
        title: "1.1 Структура тесту: Від першого рядка до запуску",
        description: `## 🎓 Вступ у світ Playwright

Уявіть, що тестування UI — це керування роботом, який на 100% точно повторює дії користувача: клікає, друкує, переглядає сторінки та перевіряє результати. Playwright — це найпотужніший пульт керування цим роботом. У цьому курсі ми навчимо вас бути професійним архітектором і пілотом таких роботів.

### Як працює тест?
Будь-який автоматизований тест — це міні-сценарій. У ньому завжди є три кроки (патерн **Arrange - Act - Assert**):
1. **Підготовка (Arrange):** Відкрити сторінку, підготувати тестові дані.
2. **Дія (Act):** Натиснути кнопку, ввести текст.
3. **Перевірка (Assert):** Переконатися, що результат правильний (з'явився текст успішної реєстрації).

У Playwright كل сценарій оформляється всередині функції \`test()\`.

## 🛠 Концепція на практиці
Функція \`test\` приймає два параметри:
1. **Назву тесту** — текстовий рядок, який ви побачите у звіті.
2. **Асинхронну функцію** — власне код вашого сценарію.

\`\`\`typescript
import { test, expect } from '@playwright/test';

test('авторизація користувача з правильними даними', async ({ page }) => {
  // Це тіло вашого тесту
  // Об'єкт 'page' — це ізольована, кришталево чиста "вкладка браузера"
  // Вона спеціально створюється тільки для цього одного тесту
});
\`\`\`

💡 **Корисні поради (Best Practices):**
* Давайте тестам максимально чіткі назви. Замість "test 1" пишіть "перевірка помилки при порожньому паролі".
* Завжди використовуйте \`async\` перед функцією тесту і \`await\` перед взаємодіями з браузером, адже всі дії відбуваються по мережі.

---
### 🎯 Ваше завдання
Давайте почнемо з найпростішого. Оголосіть порожній тест з назвою **'login test'**, що отримує об'єкт \`{ page }\`. Всередині тесту виведіть у консоль повідомлення \`console.log('Початок тесту!');\`.
`,
        code: "import { test, expect } from '@playwright/test';\n\n// Напишіть свій перший тест тут\n",
        type: "code",
        options: [
            "test('login test', async ({ page }) => { })",
            "it('login test', async () => { })",
            "describe('login test', () => { })",
            "test('login test', function(page) { })"
        ],
        correctAnswer: "test('login test', async ({ page }) => { })"
    },
    {
        title: "1.2 Навігація в інтернеті: Метод page.goto()",
        description: `## 🧭 Відкриваємо веб-сторінку

Перше, що робить реальний користувач — вводить адресу в браузері. Роботу також треба вказати, з якої сторінки починати свій маршрут. 

Для цього в Playwright використовується метод \`page.goto()\`. Це найважливіша команда, з якої починається 99% UI-тестів.

## ⚙️ Як це працює під капотом?
Коли ви викликаєте \`page.goto('url')\`:
1. Playwright відправляє команду браузеру перейти за посиланням.
2. Браузер починає завантажувати HTML.
3. **ВАЖЛИВО:** Playwright автоматично чекає, поки завантажаться основні елементи (спрацює подія \`load\`), щоб ви не намагалися взаємодіяти з порожнім екраном!

\`\`\`typescript
import { test, expect } from '@playwright/test';

test('Відкриваємо інтернет магазин', async ({ page }) => {
  // Переходимо за URL і автоматично чекаємо завершення завантаження
  await page.goto('https://makeup.com.ua/');
});
\`\`\`

## 💡 Best Practices:
* **Не використовуйте паузи \`waitForTimeout\` після \`goto\`!** Playwright достатньо розумний, щоб дочекатися ключових подій мережі самостійно.
* Зберігайте базовий URL в глобальному конфігу або змінних середовища. 

---
### 🎯 Ваше завдання
Відкрийте тестову сторінку нашого тренажеру. У вікні редактора нижче допишіть код тесту так, щоб браузер перейшов за адресою \`https://finmore.netlify.app/\`.
`,
        code: "import { test, expect } from '@playwright/test';\n\ntest('перехід на сторінку finmore', async ({ page }) => {\n  // Виконайте перехід за вказаною адресою\n  \n});",
        type: "code",
        options: [
            "await page.goto('https://finmore.netlify.app/')",
            "await page.open('https://finmore.netlify.app/')",
            "await page.navigate('https://finmore.netlify.app/')",
            "await browser.goto('https://finmore.netlify.app/')"
        ],
        correctAnswer: "await page.goto('https://finmore.netlify.app/')"
    },
    {
        title: "1.3 Базова перевірка Title: Ваш перший Assert",
        description: `## 🛡️ Чому перевірки такі важливі?
Автоматизований тест не має сенсу, якщо він нічого не перевіряє. Це буде просто бот, який бігає сторінкою. 
Операція перевірки називається **Assertion** (твердження).

Найелементарніша і найшвидша перевірка того, що сайт взагалі завантажився і ви не отримали 404 помилку чи "білий екран" — це перевірка заголовка вкладки (Title). Title — це текст, який ви бачите зверху на самій вкладці браузера.

## 🛠 Концепція на практиці
Для всіх перевірок в Playwright використовується об'єкт \`expect\`. 
Ви передаєте йому сутність (наприклад, \`page\`), а потім викликаєте відповідний матчер (наприклад, \`toHaveTitle\`).

\`\`\`typescript
import { test, expect } from '@playwright/test';

test('перевірка заголовка Google', async ({ page }) => {
  await page.goto('https://google.com');
  
  // expect має власну магію auto-retrying. 
  // Він не впаде миттєво, а почекає до 5 секунд, поки заголовок не стане правильним!
  await expect(page).toHaveTitle('Google');
  
  // Або використовуючи регулярний вираз (містить слово Google):
  await expect(page).toHaveTitle(/Google/);
});
\`\`\`

## 💡 Чому саме Playwright Assertions?
У старих фреймворках доводилося писати: "Знайди елемент -> Дочекайся його -> Витягни текст -> Порівняй з очікуваним". 
У Playwright \`expect\` автоматично повторює спроби щомілісекунди, поки умова не виконається або не закінчиться таймаут.

---
### 🎯 Ваше завдання
1. Відкрийте сторінку \`https://finmore.netlify.app/\`.
2. Перевірте, що її заголовок точно збігається зі словом \`Finmore\`.
`,
        code: "import { test, expect } from '@playwright/test';\n\ntest('перевірка title', async ({ page }) => {\n  // 1. Зробіть навігацію\n  // 2. Додайте перевірку title\n  \n});",
        type: "code",
        options: [
            "await expect(page).toHaveTitle('Finmore')",
            "await expect(page).toHaveUrl('Finmore')",
            "await page.title('Finmore')",
            "expect(title).toBe('Finmore')"
        ],
        correctAnswer: "await expect(page).toHaveTitle('Finmore')"
    },
    {
        title: "1.4 Локатори: Фундамент пошуку елементів (page.locator)",
        description: `## 🎯 Що таке локатори і як робот бачить форму?

Уявіть, що ви просите друга: "Натисни на велику синю кнопку зліва". Друг зрозуміє вас, бо він має очі. Але робот сліпий. Він бачить лише код — гігантське дерево розмітки HTML (DOM Tree).
Щоб робот міг знайти елемент, ми використовуємо **Локатори**. Це ніби координати цілі.

Ранній Playwright, як і Selenium, сильно покладався на звичайні CSS-селектори та XPath. 
Зараз метод \`page.locator()\` — це найуніверсальніший (хоч і не найпріоритетніший) інструмент.

## 🛠 Концепція на практиці

\`\`\`typescript
import { test, expect } from '@playwright/test';

test('використання CSS та XPath локаторів', async ({ page }) => {
  // CSS селектор (за класом)
  const submitButton = page.locator('.btn-primary');
  
  // CSS селектор (за ID і типом)
  const emailInput = page.locator('input#email');
  
  // XPath селектор (за атрибутом)
  const container = page.locator('//div[@data-test="container"]');
  
  // Важливо: page.locator НЕ шукає елемент миттєво в цю секунду!
  // Він лише створює "лінивий" об'єкт інструкції. 
  // Пошук в DOM почнеться тільки тоді, коли ви захочете клікнути на нього.
});
\`\`\`

## 💡 Best Practices:
* Уникайте пошуку елементів зі складною версткою на кшталт \`div > div > ul > li:nth-child(2) > a\`. Це дуже нестабільно (flaky). Дизайнер трохи змінить відступ, структура HTML зламається, і ваш тест впаде.
* Роки тому тестувальники сперечались "CSS чи XPath". Зараз Playwright взагалі радить інші, більш надійні типи локаторів (про які в наступних уроках). Але базовий \`page.locator()\` треба знати.

---
### 🎯 Ваше завдання
Знайдіть елемент форми з класом \`.login-form\` за допомогою CSS селектора та \`page.locator()\`, і збережіть його у константу \`form\`.
`,
        code: "import { test, expect } from '@playwright/test';\n\ntest('знаходження форми', async ({ page }) => {\n  // Створіть константу form і запишіть в неї локатор\n  \n});",
        type: "code",
        options: [
            "page.locator('.login-form')",
            "page.find('.login-form')",
            "page.getElement('.login-form')",
            "page.css('.login-form')"
        ],
        correctAnswer: "page.locator('.login-form')"
    },
    {
        title: "1.5 Ролі елементів: getByRole - Золотий стандарт",
        description: `## 🏆 Найкращий спосіб пошуку елементів — "За Ролями"

Колись QA інженери годинами писали складні XPath запити. Сьогодні Playwright змінив парадигму.
**Офіційна рекомендація:** Використовуйте \`getByRole()\`, завжди, коли це можливо!

Що таке Роль? Кожен елемент на сторінці має семантичне значення. Навіть люди з вадами зору за допомогою Screen Reader'ів переміщуються по сторінці "Кнопка", "Списки", "Заголовки". \`getByRole\` тестує ваш застосунок саме так, як його бачать Screen Reader'и. Тобто ви вбиваєте двох зайців: тестуєте функціонал + перевіряєте доступність (accessibility).

## 🛠 Концепція на практиці
Замість того, щоб шукати кнопку за технічним класом \`.submit-btn-v2\`, ми шукаємо її за суттю — Кнопка з текстом "Надіслати".

\`\`\`typescript
import { test, expect } from '@playwright/test';

test('клікаємо по-сучасному', async ({ page }) => {
  // Знаходимо кнопку з конкретною назвою
  const saveBtn = page.getByRole('button', { name: 'Зберегти' });
  
  // Знаходимо заголовок 1-го рівня
  const mainTitle = page.getByRole('heading', { level: 1 });
  
  // Знаходимо чекбокс
  const agreeCheck = page.getByRole('checkbox', { name: 'Я погоджуюсь' });
});
\`\`\`

## 💡 Чому це геніально?
1. **Стійкість до рефакторингу:** Програмісти можуть десять разів поміняти класи Tailwind CSS, перенести кнопку в інший DIV. Але доки це тег \`<button>\` з текстом "Зберегти" — локатор \`getByRole\` ніколи не впаде!
2. **Читабельність:** Будь-яка людина зрозуміє рядок \`getByRole('button', { name: 'Login' })\` миттєво.

---
### 🎯 Ваше завдання
Використовуючи метод \`getByRole\`, знайдіть кнопку реєстрації з текстом **'Sign up'** (зверніть увагу на регістр) і натисніть на неї методом \`.click()\`.
`,
        code: "import { test, expect } from '@playwright/test';\n\ntest('реєстрація', async ({ page }) => {\n  await page.goto('https://finmore.netlify.app/');\n  // Знайдіть кнопку реєстрації за допомогою getByRole і клікніть по ній\n  \n});",
        type: "code",
        options: [
            "page.getByRole('button', { name: 'Sign up' })",
            "page.locator('.sign-up-btn')",
            "page.xpath('//button[text()=\"Sign up\"]')",
            "page.css('button.signup')"
        ],
        correctAnswer: "page.getByRole('button', { name: 'Sign up' })"
    }
];
