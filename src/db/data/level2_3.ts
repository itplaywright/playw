export const level2Tasks_3 = [
    {
        title: "2.11 Multi-page: Робота з новими вікнами (Вкладками)",
        description: `## 🗂️ Клік -> Відкрилася нова вкладка! Що далі?

Уявіть ситуацію: на сайті є посилання на Політику конфіденційності \`<a target="_blank" href="/privacy">\`. За замовчуванням атрибут \`target="_blank"\` наказує браузеру відкрити це у новій вкладці або новому вікні.

Але якщо ви натиснете на таке посилання вашим стандартним об'єктом \`page\`, він продовжить тестувати... **оригінальну (першу) вкладку**! 
Об'єкт \`({ page })\` міцно "прибитий гвіздком" до тієї сторінки, з якої він почав. А як перевірити текст в новій вкладці?

## 🛠 Концепція на практиці
Нова вкладка — це НОВА подія, що її викидає об'єкт **Context** браузера. Нам потрібно "перехопити" (почекати на) цю подію \`'page'\` у момент натискання посилання.

Оскільки клік і відкриття вікна відбуваються **одночасно**, ми мусимо скористатись функцією \`Promise.all()\` для паралельного виконання двох дій: чекання нової сторінки і, власне, натискання кнопки-тригера.

\`\`\`typescript
import { test, expect } from '@playwright/test';

test('тест нової вкладки', async ({ context, page }) => {
  await page.goto('/home');
  
  // 1. Починаємо очікувати на нову сторінку
  const [newPage] = await Promise.all([
    // Цей проміс чекає, поки зародиться нова вкладка
    context.waitForEvent('page'),
    
    // А цей виконує клік, який є тригером
    page.getByRole('link', { name: 'Privacy Policy' }).click()
  ]);

  // 2. Тепер newPage - це повністю повноцінний об'єкт Page!
  await newPage.waitForLoadState(); // чекаємо, поки вкладка провантажиться
  
  // 3. Зверніть увагу, ми використовуємо expected на newPage
  await expect(newPage).toHaveTitle('Privacy Policy');
});
\`\`\`

## 💡 Best Practices:
Ні в якому разі не використовуйте \`page.waitForEvent('popup')\` та інше (це застаріле). Правильний сучасний спосіб перехоплення нової вкладки завжди робиться через об'єкт \`{ context }\`.

---
### 🎯 Ваше завдання
Використовуючи об'єкт \`context\`, підготуйтесь до перехоплення нової вкладки \`newPage\`, додавши очікування події з назвою \`'page'\`.
`,
        code: "import { test, expect } from '@playwright/test';\n\ntest('new tab', async ({ context, page }) => {\n  /*\n  const [newPage] = await Promise.all([\n    // зачекайте на подію відкриття 'page'\n    \n    page.click('a.blank-link')\n  ]);\n  */\n});",
        type: "code",
        options: [
            "context.waitForEvent('page')",
            "page.waitForEvent('new-tab')",
            "browser.newPage()",
            "page.switchToTab()"
        ],
        correctAnswer: "context.waitForEvent('page')"
    },
    {
        title: "2.12 Логування та Console Events",
        description: `## ⚠️ Не ігноруйте червоні помилки в консолі браузера

Ваш UI може виглядати чудово. Всі тести проходять. "Оплатити" працює.
Але якщо ви відкриєте \`F12\` (DevTools) в Chrome -> Console, ви жахнетесь: 3 мертвих ендпоінти сиплять помилками 404, а бібліотека React кричить десятками червоних \`Warning: Each child in a list should have a unique key\`.

Серйозні інженери налаштовують свої автотести так, щоб вони **"слухали"** всі \`console.error\` і повідомляли про те, що хоча UI і працює, фронтенд сиплеться!

## 🛠 Концепція на практиці
Об'єкт \`page\` "випромінює" (емитить) події. Одна з них — це подія \`'console'\`.

\`\`\`typescript
import { test, expect } from '@playwright/test';

test('слухаємо браузер', async ({ page }) => {
  // 1. Підписуємось на слухача (Event Listener) ЗАВЧАСНО!
  // Цей код ми пишемо ДО page.goto і ДО будь-яких дій.
  page.on('console', msg => {
    // Якщо розробники залишили console.error...
    if (msg.type() === 'error') {
      console.log(\`[Знайшли помилку в UI]: \${msg.text()}\`);
    }
  });

  // 2. Тепер переходимо і виконуємо тест.
  // Як тільки браузер десь виплюне помилку, відпрацює наш Listener!
  await page.goto('/buggy-page');
  
  // Можна навіть слухати помилки сторінки глобально (Crash Alert):
  page.on('pageerror', exception => {
    console.log(\`Неспіймане виключення: \${exception}\`);
  });
});
\`\`\`

## 💡 Порада з досвіду (Pro-Tip):
Якщо ви створюєте глобальний \`beforeEach\`, який буде кидати \`throw new Error()\`, якщо побачить на вашому staging-сайті помилки \`console.error\`, розробники будуть ненавидіти вас (бо тести почнуть одразу червоніти), але ваш продукт стане в 10 разів якіснішим і продуктивнішим!

---
### 🎯 Ваше завдання
Який метод з запропонованих нижче використовується для підключення "слухача" до подій консолі?
`,
        code: "import { test, expect } from '@playwright/test';\n\ntest('console log capture', async ({ page }) => {\n  // підписатись на подію 'console'\n  \n  await page.goto('https://finmore.netlify.app/');\n});",
        type: "code",
        options: [
            "page.on('console', msg => ...)",
            "page.listen('log', msg => ...)",
            "page.getLogs()",
            "console.read()"
        ],
        correctAnswer: "page.on('console', msg => ...)"
    },
    {
        title: "2.13 Анотації: Управління виконанням тесту (Skip / Fixme)",
        description: `## ⏭️ Що робити, коли тест "зламався не з вашої вини"?

Класика: розробник пише в робочому чаті: "Ой, я випадково поклав базу для реєстрації на dev-сервері, полагодити зможу аж завтра".
Ваш пайплайн Jenkins або GitHub Actions запускає тести, вони падають на Реєстрації, репорт світиться червоним (Failure), і реліз заблоковано через один сторонній баг!

Гірше рішення: видалити код тесту і сказати "поверну його завтра". (Ви забудете).
Найкраще рішення: тимчасово **"заскіпати" (skip)** цей конкретний тест, щоб він вважався "пропущеним" і не впливав на успішність всієї збірки.

## 🛠 Концепція на практиці
Ви можете керувати станом виконання тесту за допомогою **Анотацій**.

\`\`\`typescript
import { test, expect } from '@playwright/test';

// 1. Скіп без умов (статичний) прямо в описі.
// Тест не буде запускатись ВЗАГАЛІ. Дуже швидкий спосіб.
test.skip('тест не працює', async ({ page }) => {
  // Цей код взагалі не запуститься
});

// 2. Динамічний скіп!
test('залежить від ОС', async ({ page, browserName }) => {
  // Ми запускаємо тест, доходимо до цього рядка, перевіряємо умову...
  // І якщо браузер Firefox, ми ставимо статус SKIP!
  // Другим аргументом ОБОВ'ЯЗКОВО вказуйте Причину, щоб ваш лід розумів why.
  test.skip(browserName === 'firefox', 'Цей сайт працює лише в Chromium');
  
  // Код нижче виконається тільки у випадку Chromium / WebKit
});

// 3. Fixme
// Індикація того, що тест НАВМИСНО потребує ремонту.
// Працює технічно як .skip(), але у звіті позначається як "чекає виправлення".
test.fixme('написати тест колись', async ({ page }) => {
});
\`\`\`

---
### 🎯 Ваше завдання
У вас недієздатний тест, який є "Work in progress" (знаходиться в процесі розробки). За допомогою статичного методу \`test.skip\` позначте тест як такий, що пропускається безумовно (перший аргумент \`true\`).
`,
        code: "import { test, expect } from '@playwright/test';\n\ntest('wip feature', async ({ page }) => {\n  // Пропустіть цей тест явно:\n  \n});",
        type: "code",
        options: [
            "test.skip(true, 'Reason')",
            "test.ignore()",
            "test.pass()",
            "test.comment('Reason')"
        ],
        correctAnswer: "test.skip(true, 'Reason')"
    },
    {
        title: "2.14 Захист області: Групування через Describe",
        description: `## 📦 Збираємо тести "в коробки"

Коли у вашому файлі \`checkout.spec.ts\` є 40 тестів, орієнтуватись стає важко.
Але що важливіше, якщо ви покладете хук \`test.beforeEach\` у цей файл, він буде виконуватись автоматично для **всіх 40 тестів**!
Що робити, якщо 10 з цих тестів перевіряють покупки "Особистого кабінету" (і перед ними потрібна авторизація), а ще 30 тестів перевіряють "Швидку покупку як Гість" (де логінуватись заборонено)? 

Нам на допомогу приходить \`test.describe()\`. Цей метод дозволяє зібрати тести в окремі логічні ізольовані групи.

## 🛠 Концепція на практиці
Ви обгортаєте ваші тести в асинхронні функції-колбеки під парасольку \`describe\`. У кожної такої "парасольки" можуть бути СВОЇ власні хуки!

\`\`\`typescript
import { test, expect } from '@playwright/test';

// Група 1: Авторизований вхід
test.describe('Покупки як зареєстрований', () => {
  
  // Цей beforeEach лежить ВСЕРЕДИНІ групи! 
  // Він працюватиме тільки для 'user buys tv' і 'user buys watch'.
  test.beforeEach(async ({ page }) => {
    await page.goto('/login');
    // ... логінимось
  });

  test('user buys tv', async ({ page }) => { ... });
  test('user buys watch', async ({ page }) => { ... });
});

// Група 2: Покупки як Гість
test.describe('Покупки як гість (Анонімно)', () => {

  // Тут beforeEach немає зовсім (або є, але повністю інший).
  // Ці тести залишаться чистими!
  test('guest buys pc', async ({ page }) => { ... });
});
\`\`\`

## 💡 Best Practices:
Ви також можете обмежувати теги або керувати потоком виконання на рівні цілих груп (наприклад, \`test.describe.serial('Назва', () => { ... })\` змусить усі тести всередині цієї групи виконуватися суворо послідовно одне за одним, а не паралельно).

---
### 🎯 Ваше завдання
Створіть логічну групу тестів (suite/block) за допомогою конструкції \`describe\`.
`,
        code: "import { test, expect } from '@playwright/test';\n\n// Створіть спільний блок (describe)\n('Payment forms', () => {\n  test('credit card', async ({ page }) => {});\n  test('paypal', async ({ page }) => {});\n});",
        type: "code",
        options: [
            "test.describe('group', ...)",
            "test.group('group', ...)",
            "test.suite('group', ...)",
            "describe('group', ...)"
        ],
        correctAnswer: "test.describe('group', ...)"
    },
    {
        title: "2.15 М'які або поблажливі перевірки: Soft Assertions",
        description: `## 🧽 "Можна трохи пом'якше? Не стріляйте одразу!"

Ви написали довгий тест, який перевіряє сторінку:
1. Клікає "Підтвердити"
2. Перевіряє, що кнопка змінила колір (assert 1)
3. Перевіряє, що з'явився текст (assert 2)
4. Перевіряє, що відправився запит на Stripe (assert 3)

Якщо звичайний лобовий \`expect(...).toHaveClass('green')\` з першого кроку не пройде (кнопка стала синьою), ваш тест **миттєво впаде і зупиниться** (Fail-Fast behavior).
А як же інші 2 ассерти? Ми ніколи не побачимо їх результату, поки не пофіксимо перший. Це гальмує аналіз: ви фіксите колір, перезапускаєте тест (чекаєте!), щоб побачити, чи пройшов текст. Лише потім фіксите текст...

**Soft Assertions (М'які Ассерти)** збирають всі помилки в список, але дають тесту дійти живим до самого кінця.

## 🛠 Концепція на практиці
До будь-якого стандартного виклику додайте магічне слово \`.soft\`.

\`\`\`typescript
import { test, expect } from '@playwright/test';

test('м\\'яка перевірка 10 полів відразу', async ({ page }) => {
  // Навіть якщо цей чекбокс не вибраний, тест НЕ впаде тут!
  // Він просто зафіксує "помилку #1" в пам'ять.
  await expect.soft(page.locator('#terms')).toBeChecked();
  
  // Тест піде далі. Припустимо, тут він все ок.
  await expect.soft(page.locator('.title')).toHaveText('Welcome');
  
  // Тут він знову фіксує помилку (кнопки нема).
  await expect.soft(page.locator('.btn-buy')).toBeVisible();

  // От коли тест дійде до ОСТАННЬОГО рядка, тоді Playwright подивиться в пам'ять.
  // "Ага, були помилки! Кидаємо червоний статус (Fail) і у звіті виводимо ЗРАЗУ 2 проблеми!"
});
\`\`\`

## 💡 Коли це використовувати?
Використовуйте \`expect.soft\` для валідації великих візуальних форм (чи всі 5 кнопок на місці, чи всі поля заповнені правильно). Ніколи не використовуйте Soft для кроків, від яких залежить життя флоу (наприклад: чи успішно ми залогінилися - якщо ні, то йти далі просто немає сенсу, треба жорстко падати з \`expect\`).

---
### 🎯 Ваше завдання
Використайте м'яке порівняння за допомогою параметра \`soft\`, щоб перевірити значення, не завершуючи передчасно виконання тесту у разі поразки.
`,
        code: "import { test, expect } from '@playwright/test';\n\ntest('soft error', async ({ page }) => {\n  const value = 1;\n  // Модифікуйте expect, щоб зробити його м'яким\n  (value).toBe(2);\n  console.log('Цей лог відпрацює попри помилку вище!');\n});",
        type: "code",
        options: [
            "expect.soft(val).toBe(val)",
            "expect(val).soft.toBe(val)",
            "expect(val).toBe(val, { soft: true })",
            "softExpect(val).toBe(val)"
        ],
        correctAnswer: "expect.soft(val).toBe(val)"
    }
];
